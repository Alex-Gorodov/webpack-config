const i = 7;

function double(n) {
  return n * 2;
}

let x = 10;

x++;

console.log(double(i));
console.log(x);

const arrow = (y) => y + 1;

arrow();

const obj = {
  name: "alex",
};

const name = obj.name;

console.log(name);
